# Vox App

The main project documentation lives in the repository root at [`../README.md`](/Users/shreeghanesh/conductor/workspaces/transcribe-audio/lima/README.md).

Quick start:

```bash
npm install
npm run dev -- -p 3456
```

This app requires:

- `ffmpeg`
- `ffprobe`
- an OpenAI API key entered in the UI
